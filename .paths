#!/usr/bin/env bash

# rust install
if has cargo; then
    PATH_add "$CARGO_BIN"
fi

# go install
if has go; then
    PATH_add "$GOBIN"
fi

# pyenv install
PATH_add "$PYENV_BIN"

# dotnet install
PATH_add "$DOTNET_TOOLS"
